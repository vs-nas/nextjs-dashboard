import {
  Activity,
  CreditCard,
  DollarSign,
  Users,
} from "lucide-react"
import {
  Card,
  CardContent,
  CardHeader,
  CardTitle,
} from "@/components/ui/card"
import { wait } from "@/lib/utils"

export default async function Dashboard() {
  await wait(3000)

  return (
    <></>
  )
}
